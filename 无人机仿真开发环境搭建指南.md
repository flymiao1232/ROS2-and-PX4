# 无人机仿真开发环境搭建完全指南

> 适用版本：Ubuntu 24.04 LTS（VMware 虚拟机）+ ROS 2 Jazzy + Gazebo Harmonic + PX4 v1.16 + QGroundControl
> 内容来源：PX4 官方教程 ROS 环境搭建部分、Linux 学习笔记，以及实际搭建过程中遇到的真实问题与解决方案
> 编写日期：2026 年 7 月
> 使用说明：各章末尾的「常见问题」即装即查；文末附有按症状反查的「故障速查索引」。

---

# 1. 概述

## 1.1 技术栈全景

整套环境由四层组件构成，理解层次关系后再动手，能避免大部分"不知道自己在装什么"的困惑。

| 层次 | 组件 | 作用 |
|---|---|---|
| 飞控核心 | PX4 | 开源无人机自动驾驶仪软件栈，SITL 模式下完整飞行代码在电脑上运行 |
| 物理仿真 | Gazebo Harmonic | 模拟飞行器动力学与传感器输出，提供 3D 可视化 |
| 通信框架 | ROS 2 Jazzy | 连接用户程序与 PX4 的桥梁，基于 DDS 去中心化通信 |
| 桥接层 | Micro XRCE-DDS Agent | 将 PX4 的 uORB 消息映射为 ROS 2 话题，UDP 端口 8888 |

数据流向：ROS 2 控制节点 → XRCE-DDS 桥 → PX4 飞控 → Gazebo 更新物理状态 → 传感器数据回传 PX4 → 经桥接发布为 ROS 2 话题。该闭环每秒运行数百次，构成完整的仿真控制回路。

## 1.2 版本锁定清单

PX4 生态中版本兼容性比功能选择更重要，任意组件版本错位都可能导致静默失败（话题存在但收不到数据），极难排查。本指南所有版本经过实际安装验证：

| 组件 | 版本 | 说明 |
|---|---|---|
| 操作系统 | Ubuntu 24.04 LTS（noble） | ROS 2 Jazzy 官方支持平台 |
| ROS 2 | Jazzy Jalisco（LTS） | 支持至 2029 年 5 月 |
| Gazebo | Harmonic（LTS） | Jazzy 官方搭配版本 |
| PX4 固件 | v1.16.x | 正式 release，比 main 分支稳定 |
| Micro XRCE-DDS Agent | **v2.4.3** | 与 Jazzy 匹配，版本错会连不上 |
| px4_msgs | release/1.16 分支 | 必须与 PX4 固件版本严格一致 |
| Python | 3.12 | 24.04 自带 |
| setuptools（pip 用户级） | **<80（建议 79.0.1）** | 8x 新版与 colcon-core 冲突，见第 8 章 |
| QGroundControl | 最新 release AppImage | 地面站 |

**特别提醒：** 不要安装 `ros-jazzy-ros-gz`（ROS-Gazebo 话题桥）。PX4 走 uXRCE-DDS 直连，不需要它；独立版 gz-harmonic 与 ros-gz 的 vendor 包混装容易产生文件冲突。确需桥接 Gazebo 相机画面到 ROS 2 时再单独安装。

---

# 2. VMware 虚拟机安装与配置

## 2.1 安装 VMware Workstation Pro

VMware Workstation Pro 自 2024 年底起已对个人用户免费，从 Broadcom 官网下载 Windows 版安装包，按向导安装即可。安装完成后建议重启一次主机。

## 2.2 创建 Ubuntu 虚拟机

1. 从 Ubuntu 官网下载 **ubuntu-24.04.x-desktop-amd64.iso**（国内可从清华镜像站下载，速度更快）。
2. VMware 菜单：文件 → 新建虚拟机 → 典型（推荐）→ 选择"稍后安装操作系统"→ 客户机系统选 Linux / Ubuntu 64 位。
3. 命名虚拟机（如 `Ubuntu-24.04-PX4`），选择存储位置。
4. 磁盘容量建议 **80 GB 以上**（PX4 源码 + ROS 2 + Gazebo + 编译产物轻松超过 30 GB），选择"将虚拟磁盘拆分成多个文件"。
5. 完成向导后，编辑虚拟机设置，挂载 ISO 镜像，开机进入安装界面。

## 2.3 虚拟机资源配置建议

| 资源 | 建议值 | 原因 |
|---|---|---|
| 内存 | **8 GB 以上** | 编译 PX4 时内存不足会导致 make 卡死或极慢 |
| CPU | **4 核以上** | 并行编译 `make -j$(nproc)` 需要 |
| 磁盘 | 80 GB+ | 见上 |
| 3D 加速 | **勾选"加速 3D 图形"** | Gazebo 渲染需要，否则仿真画面卡顿 |
| 网络 | NAT 模式（默认） | 虚拟机经主机共享上网，无需额外配置 |

## 2.4 open-vm-tools：剪贴板共享与自适应分辨率

系统安装完成、首次进入桌面后，虚拟机和 Windows 主机之间的复制粘贴、拖放、分辨率自适应，全部依赖 open-vm-tools：（下一章会介绍linux的基础命令）

```bash
sudo apt install open-vm-tools open-vm-tools-desktop -y
sudo reboot
```

检查服务状态：

```bash
systemctl status open-vm-tools    # 显示 active (running) 才正常
sudo systemctl enable --now open-vm-tools   # 未运行时启动并设为开机自启
```

**注意事项：**

- VMware 菜单：虚拟机 → 设置 → 选项 → 客户机隔离，确认"启用复制粘贴""启用拖放"已勾选。
- 24.04 默认使用 Wayland 会话，VMware 剪贴板共享对 Wayland 支持不稳定。若重装无效，注销后在登录界面点用户名 → 右下角齿轮 → 选择 **"Ubuntu on Xorg"** 登录。

**常见问题：系统升级后剪贴板共享失效**

大版本升级（如 22.04 → 24.04）会破坏 open-vm-tools 组件，重装即可恢复：

```bash
sudo apt install --reinstall open-vm-tools open-vm-tools-desktop
sudo reboot
```

仍无效则按上面第三条换 Xorg 会话登录。

## 2.5 USB 设备直通（连接飞控）

插入 USB 设备（U 盘、飞控等）时 VMware 会弹出"Removable Devices"提示框，告知设备可分配给虚拟机。**每个 USB 设备同一时间只能属于主机或一台虚拟机。** 勾选"不再显示"后点 OK 即可。

给飞控（Pixhawk / A9）刷固件或连接 QGC 时，需要将设备切入虚拟机：

```
VMware 菜单栏 → 虚拟机 → 可移动设备 → 找到设备（PX4 FMU 或对应串口设备）→ 连接
```

连接后 Windows 主机失去该设备，虚拟机内可正常操作；断开或拔出即归还主机。

---

# 3. Linux 基础

## 3.1 文件系统结构

Linux 没有 C 盘 D 盘的概念，一切从根目录 `/` 开始。必须熟悉的核心目录：

| 目录 | 用途 | PX4 开发中的典型操作 |
|---|---|---|
| `/` | 文件系统的根 | — |
| `/home` | 普通用户主目录，类似 Windows 的 `C:\Users` | `cd ~` 回主目录 |
| `/usr` | 系统级应用程序和库 | `/usr/bin` 含大部分命令 |
| `/etc` | 系统配置文件 | 修改 apt 源、网络配置 |
| `/opt` | 可选第三方软件 | ROS 2 装在 `/opt/ros/jazzy` |
| `/var` | 日志、缓存等变化数据 | `/var/log/syslog` 查日志；`/var/crash` 崩溃记录 |
| `/tmp` | 临时文件，重启清空 | 存放下载的 deb 包 |
| `/dev` | 设备文件 | `/dev/ttyACM0` 是飞控串口 |
| `/proc` | 进程与内核信息（虚拟文件系统） | `cat /proc/cpuinfo` |
| `/boot` | 内核与引导文件 | 一般不动 |

## 3.2 路径与特殊符号

```bash
~    # 当前用户主目录，如 /home/amiao
.    # 当前目录
..   # 上级目录
-    # 上一次所在目录（cd - 跳回去）
```

绝对路径从 `/` 写起；相对路径从当前位置出发，用 `.` 和 `..` 导航。

## 3.3 常用文件操作命令

```bash
pwd                    # 查看当前所在位置
ls                     # 列出目录内容
ls -l                  # 详细列表（权限、所有者、大小、时间）
ls -a                  # 显示隐藏文件（. 开头，如 .bashrc）
ls -lah                # 组合：详细 + 人类可读 + 含隐藏文件

cd PX4-Autopilot       # 进入目录
cd ..                  # 返回上级
cd ~                   # 回主目录（直接敲 cd 同效）

mkdir ros2_ws                  # 创建目录
mkdir -p ws_ros2/src           # -p 递归创建多级目录

cp file.txt backup.txt         # 复制文件
cp -r dir1 dir2                # 复制目录（-r 递归）

mv old.txt new.txt             # 重命名
mv file.txt Documents/         # 移动

rm file.txt                    # 删除文件
rm -rf old_project/            # 递归强制删除（⚠️ 没有回收站！）
touch notes.txt                # 创建空文件
```

**`rm -rf` 是 Linux 下最危险的命令**，删除前先用 `ls` 确认目标内容，执行前再三确认路径。

## 3.4 文本查看与编辑

```bash
cat file.txt            # 显示整个文件（适合小文件）
cat -n file.txt         # 带行号
less file.txt           # 分页查看（空格翻页，/关键词 搜索，q 退出）
head -20 file.txt       # 前 20 行
tail -20 file.txt       # 后 20 行
tail -f log.txt         # 实时追踪日志新增内容（Ctrl+C 停止）
nano file.txt           # 编辑文件（Ctrl+O 保存，Ctrl+X 退出，Ctrl+W 搜索）
```

## 3.5 搜索：find、grep、which

```bash
find . -name "main.cpp"              # 按文件名查找
find . -mtime -7                     # 最近 7 天修改过的文件
grep -rn "Offboard" src/             # 递归搜索内容，-n 显示行号，-i 忽略大小写
which python3                        # 查看命令实际执行的是哪个文件
```

grep 配合管道是日常最高频的组合，如 `dpkg -l | grep libhdf5`、`ps aux | grep px4`。

## 3.6 用户与权限

Linux 分为 root（超级管理员）和普通用户。日常操作在普通用户下进行，需要管理员权限时用 `sudo` 临时提权：

```bash
whoami                 # 查看当前用户名
id                     # 查看用户 ID 和所属组
sudo apt update        # 临时以 root 权限执行单条命令
sudo -i                # 切换到 root 交互 Shell（谨慎使用）
```

**sudo 密码输入不显示任何字符（连星号都没有），这是正常设计，不是卡死。**

### 文件权限 rwx

`ls -l` 输出第一列如 `drwxr-xr-x`：第 1 位是文件类型（d 目录，- 文件），后 9 位分三组，依次为所有者 / 所属组 / 其他人的读（r）、写（w）、执行（x）权限。

```bash
chmod +x script.sh        # 添加执行权限（PX4 脚本常用）
chmod 755 script.sh       # rwxr-xr-x，可执行脚本标准权限
chmod 644 file.txt        # rw-r--r--，普通文件标准权限
sudo chown amiao:amiao file.txt    # 修改所有者
```

数字权限：r=4，w=2，x=1，三位相加。777（全员全权）仅限调试临时使用，生产环境严禁。

**常见问题：sudo 密码明明正确却提示 "Sorry, try again"**

99% 是**中文输入法处于激活状态**（屏幕右上角显示 "zh"）。密码框不回显，中文输入状态下按键被输入法拦截或转换，sudo 收到的根本不是你的密码。依次排查：

1. 按 `Super（Win 键）+ 空格` 或 `Shift`，把右上角切成 "en" 再输。
2. 仍不行的话，在普通命令行（非密码框）把密码敲一遍，肉眼检查实际输入的字符——升级系统后键盘布局可能变化。
3. 检查 CapsLock / NumLock。
4. 不要从主机复制密码粘贴——剪贴板共享若未修复，粘进去的是空内容。
5. sudo 连续失败不会锁账号，放心重试。终极手段：开机按住 Shift 进 GRUB → recovery mode → root shell → `passwd 用户名` 重置。

## 3.7 软件包管理

### apt 日常操作

```bash
sudo apt update                    # 刷新软件源列表（不安装任何东西）
sudo apt upgrade / full-upgrade    # 安装可用更新
sudo apt install git -y            # 安装软件包
sudo apt remove 包名               # 卸载（保留配置）
sudo apt purge 包名                # 完全卸载（含配置）
sudo apt autoremove --purge        # 清理不再需要的依赖
apt search 关键词                  # 搜索软件包
apt show 包名                      # 查看包详情
sudo apt install -f                # 修复依赖关系
```

### dpkg 底层工具

apt 管依赖，dpkg 管单个 .deb 包：

```bash
sudo dpkg -i package.deb           # 安装本地 deb 包
sudo dpkg --configure -a           # 完成所有未完成的配置（修复神器）
dpkg -l                            # 列出所有包（-l 是字母 L，不是数字 1）
dpkg -l | grep 关键词              # 查某个包是否安装
sudo dpkg --remove --force-remove-reinstreq 包名   # 强制卸载损坏的包
```

包状态码（`dpkg -l` 第一列）：`ii` = 正常安装；`rc` = 已卸载但残留配置文件（无害）；`iU`/`iF` = 半安装状态（有问题）。清理所有 rc 残留：

```bash
sudo dpkg --purge $(dpkg -l | awk '/^rc/ {print $2}')
```

### pip 与 Python 包

```bash
pip install --user 包名==版本号    # 安装指定版本
pip list                           # 查看已安装
```

**24.04 特有坑：** pip 默认禁止向系统 Python 环境安装包（PEP 668 外部管理环境保护），直接 `pip install` 会报 `externally-managed-environment`。解决办法是加参数：

```bash
pip install --user -U empy==3.3.4 pyros-genmsg "setuptools<80" --break-system-packages
```

**注意：** setuptools 必须限制在 80 以下——`pip install -U setuptools` 会拉到 8x 新版，与 colcon-core（要求 `setuptools<80`）冲突，后续 `colcon build` 会报依赖不兼容。若已装错，执行 `pip install --user "setuptools==79.0.1" --break-system-packages` 降级即可。

### 从源码编译

典型流程（XRCE-DDS Agent 就是这种方式）：

```bash
git clone -b v2.4.3 https://github.com/xxx/xxx.git   # 1. 下载源码
mkdir build && cd build                              # 2. 建构建目录
cmake ..                                             # 3. 配置
make -j$(nproc)                                      # 4. 编译（全部核心并行）
sudo make install                                    # 5. 安装到系统
sudo ldconfig /usr/local/lib/                        # 6. 刷新动态库缓存
```

### 常见问题：apt / dpkg 报错速查

| 报错 | 含义 | 解决 |
|---|---|---|
| `dpkg: error: unknown option -1` | `-l` 打成了数字 1 | 用字母 L：`dpkg -l` |
| `E: Invalid operation check` | apt 无 check 操作 | 正确命令是 `sudo apt-get check` |
| `E: Unable to locate package reinstall` | `--reinstall` 少打了 `--` | `sudo apt install --reinstall 包名` |
| `pre-removal script returned error` | 包的卸载脚本损坏 | `sudo dpkg --remove --force-remove-reinstreq 包名`，再 `sudo apt install -f` |
| `dependency problems - leaving unconfigured` | 缺依赖 | `sudo apt install -f` 让 apt 自动补依赖 |
| `Could not install the upgrades / upgrade has aborted` | 版本升级中断 | 勿关机！依次 `sudo dpkg --configure -a` → `sudo apt install -f` → `sudo apt full-upgrade` 补完 |

**通用修复三件套**（包管理出任何问题先跑这三条）：

```bash
sudo dpkg --configure -a
sudo apt install -f
sudo apt update && sudo apt full-upgrade
```

### 常见问题：apt update 出现 jammy（22.04）残留源

**现象：** `Hit: http://packages.ros.org/ros2/ubuntu jammy InRelease` 之类。

**原因：** 22.04 时代添加的第三方源（ROS Humble、Gazebo）在升级后残留，与新系统不匹配。

**解决：** 先查看，再删除对应文件：

```bash
ls /etc/apt/sources.list.d/
sudo rm /etc/apt/sources.list.d/ros2.list /etc/apt/sources.list.d/gazebo-stable.list
# .distUpgrade 结尾的是升级工具留的备份，可一并删除
sudo apt update
```

**注意：** 别把新装的 `ros2.sources`（noble）误删。文件名带 `.sources` 且内容是 noble 的才是新的。

### 常见问题：packages.microsoft.com 证书不受信 / TLS 握手被掐断

**现象：** `apt update` 时 VS Code 源报 `Certificate verification failed: The name in the certificate does not match`，或 `Could not handshake: The TLS connection was non-properly terminated`。

**真实原因：** 大多数情况下**不是**简单的证书问题，而是 DNS 污染——`packages.microsoft.com` 被解析到了一个错误的 IP（实际案例中被解析到了 GitHub 的服务器）。验证方法：

```bash
# 用该域名握手，看返回的证书是谁的；若最后一行是 github.com 之类，即确诊 DNS 污染
openssl s_client -connect $(getent hosts packages.microsoft.com | awk '{print $1}' | head -1):443 -servername packages.microsoft.com </dev/null 2>&1 | head -5
```

**根治方案（保留官方源）：** 通过加密 DNS（DoH）查真实 IP，写进 hosts：

```bash
# 1. 查真实 IP（看返回 JSON 中 type 为 1 的 data 字段）
curl -s "https://1.1.1.1/dns-query?name=packages.microsoft.com&type=A" -H "accept: application/dns-json"

# 2. 先用 --resolve 临时测试该 IP 可用（应返回 HTTP/2 200，证书 CN=packages.microsoft.com）
curl -Iv --resolve packages.microsoft.com:443:查到的IP https://packages.microsoft.com/repos/code/dists/stable/InRelease

# 3. 确认可用后写入 hosts
echo "查到的IP packages.microsoft.com" | sudo tee -a /etc/hosts
sudo apt update
```

**退路：** 只影响 VS Code 自动更新，与 ROS、系统健康无关。懒得修就禁用该源，VS Code 已装好的部分照常用：

```bash
sudo mv /etc/apt/sources.list.d/vscode.list /etc/apt/sources.list.d/vscode.list.bak
```

## 3.8 进程与系统管理

```bash
ps aux | grep px4        # 查找特定进程
top / htop               # 实时进程监控（q 退出，htop 需安装，更好用）
kill PID                 # 终止进程
kill -9 PID              # 强制终止（最后手段）
killall gz               # 按进程名终止（卡死的 Gazebo 常用）
df -h                    # 磁盘使用情况
du -sh ./*               # 当前目录各子目录大小
free -h                  # 内存使用
systemctl status 服务名  # 查看服务状态
systemctl --failed       # 列出所有启动失败的服务
```

## 3.9 网络基础

```bash
ip addr                  # 查看 IP 地址
ping baidu.com           # 测试外网连通性
wget 网址                # 下载文件（-c 断点续传）
curl -L -O 网址          # 下载（-L 跟随重定向）
curl -I 网址             # 只取响应头（测试连通性）
ip route | grep default  # 查看默认网关
```

### 重点：GitHub 连不上 / 时好时坏

**这是国内搭建本环境最高频的障碍**，克隆 PX4、下载依赖全部卡在 GitHub 上，先掌握方案再动手。

**现象：** `curl: (7) Failed to connect to github.com port 443`，或 git clone 卡死。

**原因：** GitHub 在国内被墙或极不稳定。**Windows 主机能访问 GitHub 不代表虚拟机能**——主机上的加速器/代理只作用于 Windows 自己，虚拟机是独立直连网络。

**方案 A：代理站中转（无需加速器）**

```bash
# 下载类命令：在 GitHub 网址前加代理前缀
curl -L -o /tmp/xxx.deb "https://ghfast.top/https://github.com/原始网址"
wget https://ghfast.top/https://raw.githubusercontent.com/原始网址

# git 全局替换：配置后所有 git clone 自动走代理（含子模块）
git config --global url."https://ghfast.top/https://github.com/".insteadOf "https://github.com/"

# 用完想取消：
git config --global --unset url."https://ghfast.top/https://github.com/".insteadOf
```

ghfast.top 不通时依次换：`ghproxy.net`、`gh-proxy.com`、`mirror.ghproxy.com`。

**方案 B：虚拟机走 Windows 的加速器（主机开着 Clash/v2ray 时）**

1. Windows 代理软件开启"允许局域网连接（Allow LAN）"，记下端口（Clash 默认 7890）。
2. 虚拟机中查询网关：`ip route | grep default`，主机 IP 一般是网段的 `.1`。
3. 配置代理：

```bash
export http_proxy=http://192.168.x.1:7890     # IP 和端口按实际情况
export https_proxy=http://192.168.x.1:7890
curl -I https://github.com                     # 测试
```

长期生效将两行 export 写入 `~/.bashrc`。

## 3.10 .bashrc 与环境变量

`~/.bashrc` 是每开一个终端就会自动执行的脚本，所有 `source ...setup.bash`、`export PATH=...` 之类的环境配置都写在这里。

```bash
echo "source /opt/ros/jazzy/setup.bash" >> ~/.bashrc   # 追加一行
source ~/.bashrc                                        # 当前终端立即生效
nano ~/.bashrc                                          # 手动编辑
grep -n "source" ~/.bashrc                              # 检查都 source 了什么
sed -i '/ros\/humble/d' ~/.bashrc                       # 删除含指定内容的行
```

**注意：** 修改 .bashrc 只对新开的终端生效，当前终端需手动 `source ~/.bashrc`。卸载软件后记得清理 .bashrc 里对应的 source 行，否则每开终端都会报 "No such file or directory"。

**常见问题：新终端总报 "/opt/ros/humble/setup.bash: No such file or directory"**

`.bashrc` 里残留了已卸载 ROS 版本的 source 行，删掉即可：

```bash
sed -i '/ros\/humble/d' ~/.bashrc
grep -n "source" ~/.bashrc    # 复查还剩哪些行
```

`#` 开头的注释行不用管。任何卸载软件后都应检查 .bashrc 残留。

## 3.11 Git 基础

```bash
git clone 网址 --recursive         # 克隆（--recursive 连同子模块，PX4 必须加）
git clone -b v1.16.0 网址          # 克隆指定分支/标签
git status                         # 查看工作区状态
git log --oneline -10              # 最近 10 条提交
git tag                            # 列出所有版本标签
git checkout v1.16.0               # 切换到指定版本
git pull                           # 拉取更新
git submodule update --init --recursive   # 子模块出问题时重新同步
```

**提示：** 用 `git checkout` 切换到某个标签后，git 会提示 "You are in 'detached HEAD' state"——这只是说明当前不挂在任何分支上，对"拉指定版本来编译"的用途毫无影响，不是报错。

---

# 4. ROS 2 Jazzy 安装

## 4.1 完整安装步骤

```bash
# 1. 准备工具，启用 universe 仓库
sudo apt update
sudo apt install software-properties-common curl -y
sudo add-apt-repository universe -y

# 2. 添加 ROS 官方源（新版方式：一个 deb 包同时配置密钥和源）
export ROS_APT_SOURCE_VERSION=$(curl -s https://api.github.com/repos/ros-infrastructure/ros-apt-source/releases/latest | grep -F "tag_name" | awk -F\" '{print $4}')

curl -L -o /tmp/ros2-apt-source.deb "https://github.com/ros-infrastructure/ros-apt-source/releases/download/${ROS_APT_SOURCE_VERSION}/ros2-apt-source_${ROS_APT_SOURCE_VERSION}.noble_all.deb"

sudo dpkg -i /tmp/ros2-apt-source.deb
```

如果第 2 步 GitHub 下载失败（国内常见），改用代理中转（见 3.9 节）：

```bash
curl -L -o /tmp/ros2-apt-source.deb "https://ghfast.top/https://github.com/ros-infrastructure/ros-apt-source/releases/download/1.1.0/ros2-apt-source_1.1.0.noble_all.deb"
sudo dpkg -i /tmp/ros2-apt-source.deb
```

```bash
# 3.（可选）换清华镜像加速；同时把 deb-src 类型去掉避免 404（见本节末尾常见问题）
sudo sed -i 's|packages.ros.org/ros2/ubuntu|mirrors.tuna.tsinghua.edu.cn/ros2/ubuntu|g' /etc/apt/sources.list.d/ros2.sources
sudo sed -i 's/Types: deb deb-src/Types: deb/' /etc/apt/sources.list.d/ros2.sources

# 4. 安装桌面版（含 RViz、rqt、示例）和开发工具
sudo apt update
sudo apt install ros-jazzy-desktop ros-dev-tools -y

# 5. 写入 bashrc，每个新终端自动加载 ROS 环境
echo "source /opt/ros/jazzy/setup.bash" >> ~/.bashrc
source ~/.bashrc

# 6. 验证
echo $ROS_DISTRO        # 应输出 jazzy
ros2 --help             # 能列出 run/launch/topic 等子命令即正常
```

**注意：** `ros2 --version` 不是有效命令（会报 `unrecognized arguments: --version`），别用它验证——这个报错经常被误以为是安装失败，其实安装是好的。

**说明：** 不换清华镜像也可以，packages.ros.org 直连速度一般但可用；几百 MB 的包下载慢时 Ctrl+C 中断后重跑同一命令，已下载部分会续传。

**常见问题：清华 ROS 镜像报 "Sources 404 Not Found"**

**现象：** `Err: http://mirrors.tuna.tsinghua.edu.cn/ros2/ubuntu noble/main Sources 404`，但二进制包正常下载。

**原因：** ros2-apt-source 的源配置声明了 `Types: deb deb-src`（二进制 + 源码），而镜像站不同步 ROS 的源码索引。

**解决：** 去掉源码类型（普通用户用不到源码包），即上面第 3 步的第二条 sed 命令：

```bash
sudo sed -i 's/Types: deb deb-src/Types: deb/' /etc/apt/sources.list.d/ros2.sources
sudo apt update
```

## 4.2 验证安装：小海龟

开两个终端：

```bash
# 终端 1：启动海龟窗口
ros2 run turtlesim turtlesim_node

# 终端 2：键盘控制（焦点在终端 2 上按方向键）
ros2 run turtlesim turtle_teleop_key
```

海龟能用方向键移动，说明 ROS 2 安装完全正常。

**注意 `ros2 run` 的格式是两段：`ros2 run 包名 可执行文件名`。** 只写包名会报 `the following arguments are required: executable_name`。在包名后加空格**连按两下 Tab** 可以列出该包所有可执行文件。Tab 补全无反应时：

```bash
sudo apt install python3-argcomplete bash-completion -y
# 装完关闭终端重开
```

## 4.3 验证安装：话题通信

```bash
# 终端 1：C++ 发布者，1Hz 发布 Hello World
ros2 run demo_nodes_cpp talker

# 终端 2：Python 订阅者，打印收到的消息
ros2 run demo_nodes_py listener
```

---

# 5. Gazebo Harmonic 安装

```bash
# 1. 添加 OSRF 官方源
sudo curl https://packages.osrfoundation.org/gazebo.gpg --output /usr/share/keyrings/pkgs-osrf-archive-keyring.gpg

echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/pkgs-osrf-archive-keyring.gpg] http://packages.osrfoundation.org/gazebo/ubuntu-stable $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/gazebo-stable.list

# 2. 安装
sudo apt update
sudo apt install gz-harmonic -y

# 3. 验证
gz sim --version
gz sim -v4 shapes.sdf    # 启动一个测试世界，能看到 3D 场景即渲染正常
```

## 常见问题：黑屏 / 卡死 / 闪退（虚拟机高发）

**典型症状：** Gazebo 窗口闪一下自动消失；弹出 "the application ruby3.2 has stopped unexpectedly"（新版 Gazebo 的 `gz` 启动器是 Ruby 写的，这个弹窗等价于 Gazebo 崩了）；但终端里 PX4 本体显示 `Startup script returned successfully` 且 `pxh>` 提示符正常——说明崩的只是渲染端，PX4 没事。

按以下顺序排查：

1. **首选：强制 CPU 软件渲染**（VMware 虚拟显卡与 Gazebo 的 ogre2 渲染引擎不兼容，这是虚拟机里闪退的最常见原因）：

   ```bash
   # 先单次验证（用 shapes.sdf 或 make px4_sitl gz_x500 均可）
   LIBGL_ALWAYS_SOFTWARE=1 gz sim shapes.sdf
   # 能稳定打开后写入 .bashrc 长期生效
   echo "export LIBGL_ALWAYS_SOFTWARE=1" >> ~/.bashrc
   source ~/.bashrc
   ```

2. 虚拟机设置确认勾选"加速 3D 图形"；勾选状态互换（开↔关）各试一次，部分 VMware 版本反着来才正常。
3. 残留进程清理：`killall gz`，或 `ps aux | grep gz` 找到 PID 后 kill。
4. 检查 OpenGL：`sudo apt install mesa-utils && glxinfo | grep "OpenGL version"`，应 ≥ 3.3。

---

# 6. PX4 工具链安装

```bash
# 1. 克隆源码（--recursive 必须加，漏掉是编译失败的最常见原因）
cd ~
git clone https://github.com/PX4/PX4-Autopilot.git --recursive
cd PX4-Autopilot

# 2. 切换到正式 release 版本
git checkout v1.16.0
git submodule update --init --recursive

# 3. 运行官方环境配置脚本
bash ./Tools/setup/ubuntu.sh
```

`ubuntu.sh` 会自动安装编译工具链、Python 依赖，并在 24.04 上自动安装 Gazebo Harmonic（已装则跳过）。跑完**重启虚拟机**。

**仿真测试：**

```bash
cd ~/PX4-Autopilot
make px4_sitl gz_x500
```

首次编译需要较长时间（虚拟机里 10~20 分钟属正常）。Gazebo 窗口弹出、出现 x500 四旋翼，即 PX4 + Gazebo 链路打通。终端出现 `pxh>` 提示符后输入 `takeoff`，飞机离地悬停 = 完整验证通过；`land` 降落，Ctrl+C 退出。

**常用编译目标：**

| 命令 | 模型 |
|---|---|
| `make px4_sitl gz_x500` | x500 标准四旋翼 |
| `make px4_sitl gz_x500_depth` | 带深度相机的 x500 |
| `make px4_sitl gz_rover` | 无人车 |
| `make px4_sitl none` | 纯 PX4，不接仿真器（配合外部仿真器或调试启动流程时用；jMAVSim 在 v1.16 已弃用，勿用旧教程的 jmavsim 目标） |

## 常见问题：编译报子模块错误

`--recursive` 漏加或子模块同步失败：

```bash
cd ~/PX4-Autopilot
git submodule update --init --recursive
# 仍失败则清缓存强制重来
git submodule deinit -f .
git submodule update --init --recursive
```

GitHub 不通时先配置 3.9 节的 git 代理替换。

---

# 7. Micro XRCE-DDS Agent（PX4 ↔ ROS 2 的桥）

**版本必须与 ROS 2 发行版匹配：Jazzy 对应 Agent v2.4.3。** 版本错误会导致 PX4 与 ROS 2 之间连不上或静默丢数据。

```bash
cd ~
git clone -b v2.4.3 https://github.com/eProsima/Micro-XRCE-DDS-Agent.git
cd Micro-XRCE-DDS-Agent
mkdir build && cd build
cmake ..
make -j$(nproc)
sudo make install
sudo ldconfig /usr/local/lib/
```

启动方式（每次仿真时在独立终端运行）：

```bash
MicroXRCEAgent udp4 -p 8888
```

---

# 8. px4_msgs 工作空间

px4_msgs 包含 PX4 全部 uORB 消息的 ROS 2 消息定义，**分支必须与 PX4 固件版本对应**（v1.16 固件配 release/1.16 分支）。

```bash
# 1. Python 依赖（24.04 必须加 --break-system-packages；setuptools 必须钉在 80 以下，否则与 colcon-core 冲突）
pip install --user -U empy==3.3.4 pyros-genmsg "setuptools<80" --break-system-packages

# 2. 建工作空间并拉取消息包和官方例程
mkdir -p ~/ws_ros2/src
cd ~/ws_ros2/src
git clone https://github.com/PX4/px4_msgs.git -b release/1.16
git clone https://github.com/PX4/px4_ros_com.git -b release/1.16

# 3. 编译
cd ~/ws_ros2
colcon build

# 4. 写入 bashrc
echo "source ~/ws_ros2/install/setup.bash" >> ~/.bashrc
source ~/.bashrc
```

**注意：** 老版本 ROS（如 Humble）时代编译过的工作空间，换 ROS 版本后必须删除 `build/`、`install/`、`log/` 三个目录重新 `colcon build`，不能直接复用。

## 常见问题：source 工作空间报 "not found: /opt/ros/humble/..."，或 ros2 run 报 symbol lookup error

**现象：**

```
not found: "/opt/ros/humble/local_setup.bash"
symbol lookup error: ... undefined symbol: _ZN6rclcpp...
[ros2run]: Process exited with failure 127
```

**原因：** 系统从 22.04 升级到 24.04 后，ROS 从 Humble 换成了 Jazzy，而旧工作空间（如 `px4_ros2_ws`）里的编译产物是当年链接 Humble 库生成的，与新 rclcpp 二进制不兼容。

**解决：** 源码分支确认匹配当前固件（release/1.16）后，删旧产物重编；旧工作空间若已被新空间替代，直接整个删掉并清理 .bashrc 残留：

```bash
cd ~/旧工作空间/src/px4_msgs && git checkout release/1.16 && git pull
cd ~/旧工作空间/src/px4_ros_com && git checkout release/1.16 && git pull
cd ~/旧工作空间
rm -rf build install log
colcon build
```

---

# 9. MAVROS 安装

```bash
sudo apt install ros-jazzy-mavros ros-jazzy-mavros-extras -y

# 安装地理数据集（MAVROS 坐标转换需要）
wget https://raw.githubusercontent.com/mavlink/mavros/master/mavros/scripts/install_geographiclib_datasets.sh
sudo bash ./install_geographiclib_datasets.sh
rm install_geographiclib_datasets.sh
```

GitHub 不通时 wget 同样加 `https://ghfast.top/` 前缀（见 3.9 节）。

---

# 10. QGroundControl 安装

```bash
sudo apt install libfuse2t64 -y    # 24.04 中 libfuse2 已更名为 libfuse2t64，装旧名会报"软件包不可用"
cd ~
wget https://github.com/mavlink/qgroundcontrol/releases/latest/download/QGroundControl-x86_64.AppImage
chmod +x QGroundControl-x86_64.AppImage
./QGroundControl-x86_64.AppImage
```

PX4 SITL 启动后 QGC 会通过 UDP 自动发现并连接仿真飞行器。

**连接真实飞控（Pixhawk / A9 等）前，还需把当前用户加入串口组，否则 QGC 打不开 /dev/ttyACM0：**

```bash
sudo usermod -aG dialout $USER
# 注销重新登录后生效
```

---

# 11. 日常启动流程与验收

全部装完后，日常仿真的标准启动顺序（三个终端）：

| 终端 | 命令 | 作用 |
|---|---|---|
| 终端 1 | `cd ~/PX4-Autopilot && make px4_sitl gz_x500` | 启动 PX4 仿真 + Gazebo |
| 终端 2 | `MicroXRCEAgent udp4 -p 8888` | 启动 DDS 桥 |
| 终端 3 | `ros2 topic list` | 应看到 `/fmu/out/...` 系列话题 |

再开一个 QGC，能看到飞行器已连接、姿态数据实时刷新。

**验收标准：** Gazebo 中出现四旋翼，`ros2 topic list` 能看到 `/fmu/out/vehicle_status`、`/fmu/out/vehicle_local_position` 等话题，`ros2 topic echo /fmu/out/vehicle_status` 有数据持续刷新，QGC 显示已连接。四项全齐 = 环境搭建完毕。

或者想看的直观点可以直接创建一个无人机仿真，尝试运行PX4官方的offboard控制示例，效果是无人机飞至5m高度悬停。如果成功说明环境配置是没有问题的。

终端1--启动QGC:

```bash
# 进入Applications目录并运行QGC
# QGC会在后台监听14550端口，等待PX4连接
cd ~/Applications && ./QGroundControl.AppImage
```

终端2--启动XRCE-DDS Agent:

```bash
# 启动DDS Agent，监听UDP 8888端口
# Agent启动后会显示 "Agent started" 并等待PX4连接
micro-xrce-dds-agent udp4 -p 8888
```

终端3-启动PX4 SITL+Gazebo仿真：

```bash
# 进入PX4源码目录并编译启动仿真
# 编译完成后自动启动Gazebo窗口，显示x500无人机
cd ~/PX4-Autopilot && make px4_sitl gz_x500
```

终端4-仿真无人机飞至5m高度悬停

```bash
# 加载工作空间环境
source ~/px4_ros2_ws/install/setup.bash

# 运行offboard控制示例
# 此节点会使飞行器解锁、起飞至5米高度并进入悬停状态 [^18^]
ros2 run px4_ros_com offboard_control
```

**运行前请确保**：Gazebo窗口可见（飞机需要地面站确认安全状态）、QGC已连接（默认需要地面站才能解锁）、Agent正在运行。你会看到x500无人机在Gazebo中缓缓升起并悬停在空中——这就是你搭建的环境的第一个成果。

## 常见问题：PX4 ↔ ROS 2 链路连不上 / 话题无数据

按以下顺序排查：

1. **Agent 版本**：Jazzy 必须 v2.4.3，`MicroXRCEAgent --version` 确认。
2. **启动顺序**：先 `make px4_sitl gz_x500`，再 `MicroXRCEAgent udp4 -p 8888`。
3. **px4_msgs 分支**：必须与固件版本一致（v1.16 ↔ release/1.16）。版本不匹配表现为话题存在但 echo 无数据——静默失败，最难排查。
4. **环境变量**：每个新终端都应有 `source /opt/ros/jazzy/setup.bash` 和 `source ~/ws_ros2/install/setup.bash`（检查 .bashrc）。
5. **重新编译**：换过分支后 `rm -rf build install log && colcon build`。

---

# 12. 系统升级 / 大版本更新后的维护

本章内容在"全新安装"时用不到，**系统做过大版本升级（如 22.04 → 24.04）后再看**。

## 12.1 升级后系统体检清单

系统大版本升级完成后，用以下 8 条命令验证系统健康状态：

```bash
lsb_release -a                                    # ① 版本应为 24.04 / noble
sudo dpkg -l | grep -v '^ii'                      # ② 除表头外应无输出
sudo apt-get check                                # ③ 无 E: 报错（注意是 apt-get，不是 apt）
sudo dpkg --configure -a && sudo apt install -f   # ④ 应显示 0 个处理动作
sudo apt update && sudo apt full-upgrade          # ⑤ 无 404 无 Failed，0 个可升级
grep -r "jammy" /etc/apt/sources.list /etc/apt/sources.list.d/   # ⑥ 无输出
systemctl --failed                                # ⑦ 0 loaded units listed
apt-mark showhold                                 # ⑧ 无输出
```

- 第 ② 条查出 `rc` 状态的包是正常的（已卸载残留配置），可无视，也可用 `sudo dpkg --purge $(dpkg -l | awk '/^rc/ {print $2}')` 清干净。
- 第 ⑥ 条查出 `#` 开头的行是注释，无害；无 `#` 的生效行才需要处理（见 3.7 节"jammy 残留源"）。

## 12.2 升级过程中弹 "System program problem detected"

**现象：** apport 崩溃报告器弹窗，问是否发送报告。

**原因：** 升级过程中某个旧组件被替换时崩溃触发，常见且基本无害。

**解决：** 点 Cancel / Don't send 即可，不要点升级窗口的 Cancel，不要强制关机，让升级跑完。重启后若反复弹，清理崩溃记录：

```bash
sudo rm /var/crash/*
```

## 12.3 升级后的典型后遗症速查

| 后遗症 | 解决位置 |
|---|---|
| 主机↔虚拟机剪贴板失效 | 2.4 节 |
| apt 源里残留 jammy（22.04）条目 | 3.7 节 |
| .bashrc 报 humble 路径找不到 | 3.10 节 |
| 旧工作空间 symbol lookup error | 第 8 章 |

---

# 13. 安装顺序速查表

全部流程一图流，按序执行：

| 步骤 | 内容 | 验收标准 |
|---|---|---|
| 1 | VMware 创建虚拟机 + 安装 Ubuntu 24.04 | 正常进入桌面 |
| 2 | open-vm-tools-desktop | 主机↔虚拟机复制粘贴正常 |
| 3 | ROS 2 Jazzy（ros-apt-source + desktop） | `echo $ROS_DISTRO` 输出 jazzy，小海龟能动 |
| 4 | Gazebo Harmonic | `gz sim --version` 正常 |
| 5 | PX4 源码 + ubuntu.sh + 重启 | `make px4_sitl gz_x500` 弹出仿真 |
| 6 | Micro XRCE-DDS Agent v2.4.3 | `MicroXRCEAgent udp4 -p 8888` 能启动 |
| 7 | px4_msgs 工作空间 | `colcon build` 无错误 |
| 8 | MAVROS | apt 安装完成 |
| 9 | QGroundControl | AppImage 启动，自动连接仿真 |
| 10 | 三终端联合验证 | `/fmu/out/...` 话题有数据，QGC 已连接 |

---

# 14. 故障症状速查索引

遇到报错先看这里，按症状找到对应章节的解决方案：

| 症状 | 去哪看 |
|---|---|
| sudo 密码正确却提示 Sorry, try again | 3.6 节 |
| 主机↔虚拟机无法复制粘贴 | 2.4 节 |
| GitHub 连不上 / git clone 卡死 | 3.9 节 |
| apt update 出现 jammy 源 | 3.7 节 |
| packages.microsoft.com 证书报错 | 3.7 节 |
| apt / dpkg 各种 E: 报错 | 3.7 节报错速查表 |
| pip 报 externally-managed-environment | 3.7 节 |
| 新终端报 humble 路径找不到 | 3.10 节 |
| `ros2 --version` 报错 | 4.1 节（该命令不存在，不是故障） |
| 清华 ROS 镜像 Sources 404 | 4.1 节 |
| Gazebo 黑屏 / 闪退 / ruby3.2 崩溃弹窗 | 第 5 章 |
| PX4 编译报子模块错误 | 第 6 章 |
| colcon build 报 setuptools 依赖冲突 | 第 8 章 / 3.7 节 |
| ros2 run 报 symbol lookup error | 第 8 章 |
| `/fmu/out/...` 话题存在但无数据 | 第 11 章 |
| QGC 打不开 /dev/ttyACM0（真飞控） | 第 10 章 |
| 系统升级后整体体检 | 第 12 章 |
| 升级弹 "System program problem detected" | 12.2 节 |

---

> 本文档基于实际搭建过程整理，覆盖从虚拟机创建到仿真跑通的完整链路。遇到本文档未覆盖的报错时，保留终端完整输出（尤其是 E: / error 开头的行）再排查，比凭记忆描述问题效率高得多。
